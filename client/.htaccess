<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # If request is not for a real file or directory, serve index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
